spring:
  datasource:
    username: root
    password: duzejiang
    url: jdbc:mysql://localhost:3306/jpa?useSSL=false&useUnicode=true&characterEncoding=utf8
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
    #更新或创建数据表结构
      ddl-auto: update
    #控制台显示sql
    show-sql: true